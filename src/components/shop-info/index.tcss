
.header_bar{
  padding: 0rpx 70rpx;
  margin-top: 120rpx;
  font-size: 50rpx;
   color: #00AB56;
   font-weight: 800;
   display: flex;
   justify-content: space-between;
   align-items: center;
  
}

.logo_header{
  width: 50rpx;
  height: 50rpx;
}